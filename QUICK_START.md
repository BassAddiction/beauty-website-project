# ⚡ Быстрый старт Speed VPN

Три способа развернуть проект на выбор:

---

## 🐳 Вариант 1: Docker Compose (рекомендуется, 5 минут)

### Шаг 1: Подготовка

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/speedvpn.git
cd speedvpn

# Настройте переменные окружения
cp .env.example .env
nano .env  # Измените пароли и API ключи
```

### Шаг 2: Сборка frontend

```bash
bun install
bun run build
```

### Шаг 3: Запуск

```bash
# С помощью Makefile (удобнее)
make deploy

# Или напрямую через docker-compose
docker-compose up -d
```

### Готово! 🎉

- Frontend: http://localhost
- Backend: http://localhost:3000
- Adminer (БД): http://localhost:8080

**Полная документация:** [DOCKER_DEPLOYMENT.md](./DOCKER_DEPLOYMENT.md)

---

## 🖥️ Вариант 2: Ручное развертывание на сервере (30 минут)

Подходит для production на VPS/Dedicated сервере.

**Полная инструкция:** [DEPLOYMENT.md](./DEPLOYMENT.md)

### Кратко:

1. Установите зависимости (Node.js, PostgreSQL, Nginx)
2. Создайте базу данных и примените миграции
3. Соберите frontend и настройте Nginx
4. Разверните backend функции (Serverless или собственный сервер)
5. Настройте SSL через Let's Encrypt
6. Настройте вебхуки ЮKassa, Remnawave, Unisender

---

## 📦 Вариант 3: Makefile команды (если Docker уже настроен)

```bash
make help           # Показать все команды
make install        # Установить зависимости
make build          # Собрать Docker образы
make start          # Запустить сервисы
make stop           # Остановить сервисы
make logs           # Показать логи
make logs-backend   # Логи backend
make logs-nginx     # Логи nginx
make health         # Проверить здоровье
make backup         # Backup базы данных
make update         # Обновить приложение
```

---

## 🔧 Настройка переменных окружения

Минимально необходимо настроить в `.env`:

```env
# База данных
POSTGRES_PASSWORD=your_secure_password

# Админка
ADMIN_PASSWORD=your_admin_password

# ЮKassa
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=live_your_secret_key

# Remnawave (VPN)
REMNAWAVE_API_URL=https://your-panel.com
REMNAWAVE_API_TOKEN=your_token
```

Полный список параметров в `.env.example`

---

## 🎯 Production чеклист

После запуска:

- [ ] Смените ВСЕ пароли в `.env`
- [ ] Настройте домен (A-записи в DNS)
- [ ] Получите SSL сертификаты (Let's Encrypt)
- [ ] Настройте вебхуки в ЮKassa
- [ ] Настройте вебхуки в Remnawave (если есть)
- [ ] Настройте вебхуки в Unisender
- [ ] Настройте автоматический backup БД
- [ ] Настройте мониторинг (Grafana, Prometheus)
- [ ] Настройте firewall (ufw)
- [ ] Проверьте логи на ошибки

---

## 🆘 Быстрая помощь

### Проблемы с Docker?

```bash
# Перезапустить всё
make restart

# Посмотреть логи
make logs

# Проверить статус
make status

# Проверить здоровье
make health
```

### Проблемы с БД?

```bash
# Подключиться к PostgreSQL
make shell-db

# Backup
make backup

# Восстановить
make restore
```

### Очистка и переустановка

```bash
# Мягкая очистка
make clean

# Полная очистка (удаляет ВСЕ данные!)
make clean-all
```

---

## 📚 Документация

- **Docker развертывание:** [DOCKER_DEPLOYMENT.md](./DOCKER_DEPLOYMENT.md)
- **Ручное развертывание:** [DEPLOYMENT.md](./DEPLOYMENT.md)
- **Telegram сообщество:** https://t.me/+QgiLIa1gFRY4Y2Iy

---

## 🚀 Команды для ежедневной работы

```bash
# Разработка
make dev              # Запуск с Adminer для отладки БД
make logs-backend     # Смотреть логи backend
bun run dev          # Frontend в режиме разработки

# Production
make prod            # Запуск в production
make update          # Обновить код и перезапустить
make backup          # Ежедневный backup БД
make health          # Проверить статус всех сервисов

# Мониторинг
make stats           # Использование CPU/RAM
make logs            # Все логи
docker stats         # Детальная статистика
```

---

**Выбери удобный вариант и начинай! 🎉**
