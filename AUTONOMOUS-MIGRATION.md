# 🚀 Миграция на автономный режим

Этот проект настроен для безопасного переключения между режимами работы:
- **Режим poehali.dev** (по умолчанию) — все работает через облако poehali.dev
- **Автономный режим** — полностью независимая работа на вашем сервере

## ✅ Что уже сделано

### 1. Централизованная конфигурация
- Создан `/src/config/api.ts` — все URL в одном месте
- Все frontend файлы используют импорты из конфига
- Изменения URL теперь происходят в одном файле

### 2. Переменные окружения
- Создан `.env` с настройками по умолчанию (poehali.dev)
- Обновлен `.env.example` с инструкциями
- Поддержка `VITE_API_BASE_URL` и `VITE_CDN_BASE_URL`

### 3. Очистка от платформенных скриптов
- Удалены `pp-min-2.js`, `telemetry-min.js`, `inspector-min.js` из `index.html`
- Tracking codes теперь используют переменную окружения
- CDN ссылки в `index.html` заменены на локальные `/logo.jpg`

### 4. Замененные файлы
**27 файлов обновлено:**
- ✅ Все `/src/pages/*.tsx`
- ✅ Все `/src/components/**/*.tsx`
- ✅ `/index.html`
- ✅ Конфигурация API

## 📋 Инструкция по миграции

### Шаг 1: Текущий режим (ничего не делать)
По умолчанию всё работает через poehali.dev:
```env
VITE_API_BASE_URL=https://functions.poehali.dev
VITE_CDN_BASE_URL=https://cdn.poehali.dev
```

**Сайт продолжает работать как раньше** ✅

---

### Шаг 2: Подготовка к автономности

#### 2.1. Разверните backend на своем сервере
Используйте Docker Compose из проекта:
```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL (база данных)
- Redis (кеширование)
- Backend server (все функции из `/backend`)

#### 2.2. Скачайте CDN файлы
Скачайте логотип и иконки в `/public`:
```bash
# Логотип
wget https://cdn.poehali.dev/files/299c507f-f10f-4048-a927-9fa71def332e.jpg -O public/logo.jpg

# Иконки клиентов (опционально)
wget https://cdn.poehali.dev/files/3a0045b1-8f62-461c-946f-ea67286d8040.png -O public/client-windows.png
wget https://cdn.poehali.dev/files/94a03603-e00b-4b48-bedb-8e78efd9f6f5.png -O public/client-macos.png
wget https://cdn.poehali.dev/files/f73c3d53-4f72-4e32-9ec2-03c22c3e0af7.png -O public/client-android.png
wget https://cdn.poehali.dev/files/efbc1ee1-7ead-4f28-bd0d-3f4a1a13b09f.png -O public/client-linux.png
```

#### 2.3. Обновите CDN конфигурацию
Измените `/src/config/api.ts`:
```typescript
// Было:
CLIENT_ICONS: {
  WINDOWS: `${CDN_BASE}/files/3a0045b1-8f62-461c-946f-ea67286d8040.png`,
  // ...
}

// Стало:
CLIENT_ICONS: {
  WINDOWS: '/client-windows.png',
  MACOS: '/client-macos.png',
  IOS: '/client-windows.png',
  ANDROID: '/client-android.png',
  LINUX: '/client-linux.png',
}
```

---

### Шаг 3: Переключение на автономный режим

Измените `.env`:
```env
# Было:
VITE_API_BASE_URL=https://functions.poehali.dev
VITE_CDN_BASE_URL=https://cdn.poehali.dev

# Стало:
VITE_API_BASE_URL=https://your-domain.com/api
VITE_CDN_BASE_URL=https://your-domain.com
```

Пересоберите frontend:
```bash
npm run build
```

**Готово!** Теперь проект работает полностью автономно 🎉

---

## 🔄 Откат на poehali.dev

Если что-то пошло не так, просто верните `.env`:
```env
VITE_API_BASE_URL=https://functions.poehali.dev
VITE_CDN_BASE_URL=https://cdn.poehali.dev
```

Пересоберите:
```bash
npm run build
```

Всё вернется к работе через poehali.dev ✅

---

## 🧪 Тестирование

### Проверка API endpoints
```bash
# Проверьте что backend работает
curl https://your-domain.com/api/plans

# Должен вернуть JSON с тарифами
```

### Проверка CDN
```bash
# Проверьте что логотип доступен
curl -I https://your-domain.com/logo.jpg

# Должен вернуть 200 OK
```

---

## 📊 Что НЕ нужно менять

### Backend остается как есть
Файл `/backend/func2url.json` НЕ трогаем — он используется только для поехали.dev деплоя.

Backend функции используют переменные окружения:
```python
db_url = os.environ.get('DATABASE_URL', '')  # ✅ Правильно
```

### Маршруты в коде
Все маршруты уже обновлены автоматически через `/src/config/api.ts`.

---

## 🆘 Проблемы и решения

### Проблема: 404 на API запросы
**Решение:** Проверьте что backend server запущен и доступен по URL из `.env`

### Проблема: Изображения не загружаются
**Решение:** Убедитесь что файлы скачаны в `/public` и доступны

### Проблема: Старые URL в коде
**Решение:** Все должно быть заменено. Проверьте через поиск:
```bash
grep -r "functions.poehali.dev" src/
grep -r "cdn.poehali.dev" src/
```

---

## 📞 Поддержка

Если возникли вопросы — напишите в Telegram: [@speed_vpn_support](https://t.me/speed_vpn_support)
