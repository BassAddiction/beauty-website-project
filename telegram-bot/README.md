# 🤖 Speed VPN Telegram Bot

Telegram бот для продажи VPN подписок с автоматическим приемом платежей и выдачей конфигураций.

---

## 📋 Возможности бота

✅ **Покупка подписок** - 3 тарифа (месячный, квартальный, годовой)  
✅ **Прием платежей** - Интеграция с ЮKassa через Telegram Payments  
✅ **Автоматическая выдача** - Мгновенная активация после оплаты  
✅ **Управление подпиской** - Проверка статуса и срока действия  
✅ **Выдача конфигураций** - WireGuard конфиг файлы для подключения  
✅ **База данных** - Синхронизация с основным проектом  

---

## 🚀 Как запустить бота

### Шаг 1: Создайте Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/newbot`
3. Придумайте **имя** для бота (например: `Speed VPN Bot`)
4. Придумайте **username** для бота (например: `speedvpn_bot`)
5. BotFather даст вам **токен** - сохраните его

Пример токена:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

---

### Шаг 2: Настройте платежи в Telegram

Бот поддерживает платежи через **Telegram Payments API**.

#### Вариант 1: ЮKassa (рекомендуется для России)

1. Зайдите в [личный кабинет ЮKassa](https://yookassa.ru/)
2. Перейдите: **Настройки → Интеграции → Telegram**
3. Скопируйте **Payment Provider Token**

#### Вариант 2: Другие провайдеры

Telegram поддерживает:
- Stripe
- PayPal
- Сбербанк
- Другие через официальный [список провайдеров](https://core.telegram.org/bots/payments#supported-payment-providers)

---

### Шаг 3: Добавьте токены в .env файл

Откройте файл `.env` в корне проекта и добавьте:

```env
# Токен вашего бота из BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789

# Токен платежного провайдера (например, из ЮKassa)
PAYMENT_PROVIDER_TOKEN=381764678:TEST:12345
```

---

### Шаг 4: Запустите бота

Бот запускается автоматически вместе с проектом через Docker Compose.

```bash
# Перезапустите проект с ботом
docker compose up -d --build

# Проверьте что бот запустился
docker compose logs telegram-bot
```

Вы должны увидеть:
```
INFO - Bot started successfully!
```

---

## 📱 Как пользоваться ботом

### Команды бота

- `/start` - Главное меню бота
- `/config` - Получить конфигурацию WireGuard
- `/help` - Помощь и FAQ

### Главное меню

После `/start` пользователь видит кнопки:

1. **🛒 Купить подписку** - Выбор тарифа и оплата
2. **📊 Моя подписка** - Информация о текущей подписке
3. **❓ Помощь** - FAQ и поддержка

### Процесс покупки

1. Пользователь нажимает **🛒 Купить подписку**
2. Выбирает один из 3 тарифов:
   - 💳 Месячный - 299 ₽/мес
   - 💎 Квартальный - 699 ₽ (22% скидка)
   - 🎁 Годовой - 1999 ₽ (44% скидка)
3. Нажимает **💳 Оплатить**
4. Telegram открывает форму оплаты
5. После успешной оплаты бот:
   - Создает подписку в базе данных
   - Отправляет инструкцию по подключению
   - Генерирует WireGuard конфигурацию

---

## 🗄️ База данных

Бот использует **ту же PostgreSQL базу данных**, что и основной проект.

### Таблицы, которые использует бот:

#### `users` - Пользователи Telegram
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    telegram_id BIGINT UNIQUE NOT NULL,
    username VARCHAR(255),
    full_name VARCHAR(255),
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### `subscriptions` - Подписки пользователей
```sql
CREATE TABLE subscriptions (
    id SERIAL PRIMARY KEY,
    user_id INT REFERENCES users(id),
    plan VARCHAR(50) NOT NULL,
    status VARCHAR(50) DEFAULT 'active',
    payment_id VARCHAR(255),
    created_at TIMESTAMP DEFAULT NOW(),
    expires_at TIMESTAMP NOT NULL
);
```

---

## ⚙️ Настройка тарифов

Тарифы находятся в файле `telegram-bot/bot.py`:

```python
PLANS = {
    'monthly': {
        'name': 'Месячный',
        'price': 299,           # Цена в рублях
        'duration_days': 30,    # Длительность в днях
        'description': '...'
    },
    'quarterly': {
        'name': 'Квартальный',
        'price': 699,
        'duration_days': 90,
        'description': '...'
    },
    'yearly': {
        'name': 'Годовой',
        'price': 1999,
        'duration_days': 365,
        'description': '...'
    }
}
```

**Как изменить цены:**

1. Откройте `telegram-bot/bot.py`
2. Найдите словарь `PLANS`
3. Измените значения `price` и `duration_days`
4. Перезапустите бота:
   ```bash
   docker compose restart telegram-bot
   ```

---

## 🔧 Устранение проблем

### Бот не запускается

**Проверьте логи:**
```bash
docker compose logs telegram-bot
```

**Частые ошибки:**

1. **"TELEGRAM_BOT_TOKEN not found"**
   - Проверьте что токен добавлен в `.env` файл
   - Перезапустите: `docker compose up -d --build`

2. **"Connection to database failed"**
   - Проверьте что PostgreSQL запущен: `docker compose ps`
   - Проверьте `DATABASE_URL` в `.env`

3. **"Invalid bot token"**
   - Убедитесь что токен скопирован правильно из BotFather
   - Токен должен начинаться с цифр и содержать `:`

### Платежи не работают

1. **"Payment provider token not found"**
   - Добавьте `PAYMENT_PROVIDER_TOKEN` в `.env`
   - Получите токен в личном кабинете ЮKassa

2. **"Pre-checkout query failed"**
   - Проверьте что токен провайдера валидный
   - Убедитесь что магазин ЮKassa активирован

3. **"Payment failed"**
   - Проверьте лимиты в ЮKassa
   - Убедитесь что используете БОЕВОЙ токен (не тестовый)

### База данных не синхронизируется

**Проверьте подключение:**
```bash
# Зайдите в контейнер бота
docker exec -it speedvpn-telegram-bot bash

# Проверьте переменную DATABASE_URL
echo $DATABASE_URL

# Попробуйте подключиться к БД
python3 -c "import psycopg2; conn = psycopg2.connect('$DATABASE_URL'); print('OK')"
```

---

## 📊 Мониторинг

### Просмотр логов бота
```bash
# Все логи
docker compose logs telegram-bot

# Только последние 50 строк
docker compose logs telegram-bot --tail=50

# Следить за логами в реальном времени
docker compose logs -f telegram-bot
```

### Перезапуск бота
```bash
# Перезапустить только бота
docker compose restart telegram-bot

# Полная пересборка и перезапуск
docker compose up -d --build telegram-bot
```

### Остановка бота
```bash
# Остановить бота (но не удалять)
docker compose stop telegram-bot

# Удалить контейнер бота
docker compose rm -f telegram-bot
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Никогда не коммитьте `.env` файл**
   - Файл уже в `.gitignore`
   - Не делитесь токенами публично

2. **Используйте боевые токены только на продакшене**
   - Для разработки используйте тестовые токены
   - Отделяйте тестовую и боевую среды

3. **Регулярно проверяйте логи**
   - Следите за подозрительной активностью
   - Проверяйте неудачные платежи

4. **Обновляйте зависимости**
   ```bash
   # Обновить python-telegram-bot
   pip install --upgrade python-telegram-bot
   
   # Пересобрать контейнер
   docker compose build telegram-bot
   ```

---

## 🆘 Поддержка

Если возникли проблемы с ботом:

1. **Проверьте документацию** - этот README.md
2. **Посмотрите логи** - `docker compose logs telegram-bot`
3. **Сообщество** - https://t.me/+QgiLIa1gFRY4Y2Iy
4. **Email** - support@speedvpn.io

---

## 📝 Лицензия

Этот бот является частью проекта Speed VPN и использует те же условия лицензирования.
