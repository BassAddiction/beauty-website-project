# 🚀 Быстрая настройка Telegram бота

## Шаг 1: Создайте бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/newbot`
3. Придумайте **имя** для бота (например: `Speed VPN Bot`)
4. Придумайте **username** для бота (например: `speedvpn_bot`)
5. BotFather даст вам **токен** - скопируйте его

Пример токена:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

---

## Шаг 2: Настройте платежи в Telegram

### Для России - ЮKassa:

1. Зайдите в [личный кабинет ЮKassa](https://yookassa.ru/)
2. Перейдите: **Настройки → Интеграции → Telegram**
3. Скопируйте **Payment Provider Token**

Пример токена:
```
381764678:TEST:12345
```

> ⚠️ **Важно:** Для боевого режима используйте LIVE токен, не TEST!

---

## Шаг 3: Добавьте токены в .env

Откройте файл `.env` в корне проекта и добавьте:

```env
# Токен вашего бота из BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789

# Токен платежного провайдера из ЮKassa
PAYMENT_PROVIDER_TOKEN=381764678:TEST:12345
```

---

## Шаг 4: Примените миграцию базы данных

Бот использует отдельные таблицы для хранения пользователей и платежей.

Миграция уже создана в `db_migrations/V0028__create_telegram_users_and_payments.sql`

Она применится автоматически при запуске проекта.

---

## Шаг 5: Запустите бота

```bash
# Перезапустите проект
docker compose up -d --build

# Проверьте логи бота
docker compose logs telegram-bot -f
```

Вы должны увидеть:
```
INFO - Bot started successfully!
```

---

## ✅ Готово!

Теперь найдите вашего бота в Telegram и отправьте `/start`

Бот будет:
- Показывать актуальные тарифы из вашей базы данных
- Принимать платежи через Telegram Payments
- Сохранять информацию о покупках
- Синхронизироваться с основным сайтом

---

## 🔧 Как это работает

### Тарифы

Бот загружает тарифы из той же таблицы `subscription_plans`, что и сайт.

Чтобы изменить тарифы:
1. Зайдите в админ-панель сайта
2. Отредактируйте тарифы
3. Изменения сразу отобразятся в боте

### Платежи

1. Пользователь выбирает тариф
2. Telegram открывает форму оплаты
3. После успешной оплаты бот:
   - Сохраняет платеж в `telegram_payments`
   - Отправляет подтверждение пользователю

### База данных

Бот использует 2 таблицы:

**telegram_users** - пользователи бота:
- `telegram_id` - ID в Telegram
- `username` - имя пользователя
- `full_name` - полное имя

**telegram_payments** - платежи:
- `telegram_user_id` - кто оплатил
- `plan_id` - какой тариф
- `payment_id` - ID платежа в Telegram
- `amount` - сумма
- `status` - статус (pending/succeeded/failed)

---

## 🆘 Проблемы?

### Бот не запускается

```bash
# Проверьте логи
docker compose logs telegram-bot

# Типичные ошибки:
# "TELEGRAM_BOT_TOKEN not found" - добавьте токен в .env
# "Connection to database failed" - проверьте DATABASE_URL
```

### Платежи не работают

```bash
# Проверьте что токен провайдера добавлен
grep PAYMENT_PROVIDER_TOKEN .env

# Проверьте что используете правильный токен:
# TEST - для тестирования
# LIVE - для реальных платежей
```

### База данных

```bash
# Проверьте что миграция применилась
docker compose exec postgres psql -U speedvpn_user -d speedvpn_db -c "\dt telegram_*"

# Должны увидеть:
# telegram_users
# telegram_payments
```

---

## 📝 Команды для управления

```bash
# Перезапустить только бота
docker compose restart telegram-bot

# Пересобрать и запустить
docker compose up -d --build telegram-bot

# Остановить бота
docker compose stop telegram-bot

# Логи в реальном времени
docker compose logs -f telegram-bot
```
